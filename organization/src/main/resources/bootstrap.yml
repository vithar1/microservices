management:
  endpoints:
    web:
      exposure:
        include: refresh
spring:
  application:
    name: organization-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://configserver:8071
server:
  port: 8081